export interface Contact {
  id: string;
  name: string;
  number: string;
  agenteHabilitado?: boolean;
}

export interface Group {
  id: string;
  name: string;
  number: string;
  agenteHabilitado?: boolean;
} 